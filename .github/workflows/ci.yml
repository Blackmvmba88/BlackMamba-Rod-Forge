name: CI

on:
  push:
  pull_request:

jobs:
  test:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: ["3.11", "3.12"]

    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}
          cache: pip
      - name: Install
        run: pip install -e '.[dev]'
      - name: Test autonomous core
        run: pytest --cov=rodforge --cov-report=term-missing
      - name: Smoke-test CLI
        run: rodforge run --config configs/project.yaml --executor dry-run
